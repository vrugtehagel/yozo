#!/bin/bash

# This script just builds everything.

# Move to the project root
cd "$(dirname "$0")/.."

# Delete dist/ for a clean slate
rm -rf ../dist

# And run the build scripts
./bin/build-site
./bin/build-yozo
